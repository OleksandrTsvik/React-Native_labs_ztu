import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontSize: 18,
    fontWeight: "600",
    textAlign: "center",
    marginBottom: 8,
  },
  formContainer: {
    gap: 16,
    paddingBottom: 8,
  },
  submitButton: {
    paddingVertical: 10,
    backgroundColor: "#0073FF",
    borderRadius: 4,
  },
  submitButtonText: {
    fontSize: 12,
    textAlign: "center",
    color: "#fff",
  },
});
