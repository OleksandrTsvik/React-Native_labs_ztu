import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  label: {
    fontSize: 12,
    marginBottom: 4,
  },
  textInput: {
    paddingHorizontal: 8,
    paddingVertical: 4,
    borderWidth: 2,
    borderRadius: 4,
  },
  errorMessage: {
    color: "#dc4446",
  },
});
